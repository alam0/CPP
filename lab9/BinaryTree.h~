#ifndef _BINARYTREE_H_
#define _BINARYTREE_H_

//BinaryTree.h
#include "Node.h"     // contains the node information
#include <iostream>   // for using cout
#include <vector>
#include <cstdlib>
#include <algorithm>
#include <iterator>

class BinaryTree {
	public:
	 BinaryTree();
	 ~BinaryTree();
	 
	 
	 search(int);
	 inorder();
	 preorder();
	 postorder();
	private:
	 Node<int> *first;  
	 //Node<int> *newNode(const &);
	 
};

//Read in a file and create vector of pointer to Nodes
BinaryTree(){
	
	vector<Node<int> *> firstlevel; 
}






